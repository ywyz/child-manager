 

# M0 阻塞项统一修复计划（Codex + Trae）

> 创建日期：2026-07-13
> 合并说明：以 `codex-fix.md` 为主稿，吸收 Trae 的反思与 FR-031 示例，并修正数据最小化、T002 时态、契约引用及 P2/P3 归类。
> 依据报告：[codex-trae联合审查报告20260713.md](codex-trae联合审查报告20260713.md)
> 操作边界：本文只提供修复建议，不授权修改代码、提交、推送、创建或切换分支、创建 Issue 或改写 Git 历史。

建议按“内容修复 → 状态收敛 → 专项验证 → 历史清理 → 最终基线”的顺序处理。不得先修改状态或关闭门禁，否则会出现文档声称完成、验证证据仍未通过的情况。

## 1. P0：修复普通文档阻断项

### 1.1 修订模板说明，关闭 M0-G2

目标文件：`templates/teacherplan/一日活动计划系统说明.md`

建议一次完成以下修改：

- 节假日接口改为 `https://timor.tech/api/holiday/info/{YYYY-MM-DD}`。
- 删除集体活动和反思中的 `[[字段名]]` 文本标记。
- 明确新增集体活动环节使用结构化字段 `is_ai_added=true`，正文不得写入“新增”等标记文字。
- 反思字段统一为：
  - `highlights`：活动亮点；
  - `issues`：存在问题；
  - `adjustments`：调整策略。
- 明确三个字段分别执行 Unicode NFKC 规范化后再拼接。
- 按完整 Unicode 码点计数，空格、标点和 emoji 均计入，总计不超过 200。
- 删除 `U+0000–U+FFFF`、超出范围可能截断或替换的 BMP 旧规则。
- 明确 Word 导出固定为三行映射，不把内部字段名或结构标记写入正文。

可吸收 Trae 建议中的具体反思说明，但必须收紧外部 AI 数据范围。建议文本如下：

```markdown
#### 一日活动反思填写说明

1. 一日活动反思只能在前五个上游栏目满足结构化完整性后，由教师显式发起生成。
2. 系统只向外部 AI 发送生成反思必需的五个上游栏目和最小必要上下文，默认排除教师账号、幼儿身份信息及其他无关正文。
3. AI 返回 `highlights`、`issues`、`adjustments` 三个结构化字段，不使用 `[[字段名]]` 文本标记。
4. 三个字段值分别执行 Unicode NFKC 规范化后再拼接，按完整 Unicode 码点计数；空格、标点和 emoji 均计入，合计不得超过 200。
5. Word 导出依次映射为“活动亮点”“存在问题”“调整策略”三行，不输出内部字段名或结构标记。
```

验收标准：

- `rg` 不再命中旧 HTTP 地址、`[[...]]` 和 BMP 截断规则。
- 新 HTTPS 地址、三个字段、`is_ai_added=true`、NFKC 及 200 限制均有明确命中。
- `.docx` 模板不做任何修改，SHA-256 保持不变。

### 1.2 补充 FR-031，关闭 M0-G1 的快照原因缺口

目标文件：`specs/001-daily-activity-plan/spec.md`

吸收 Trae 的具体替换示例并修正恢复顺序和采用条件后，建议文本如下：

```markdown
- **FR-031**: 自动保存只更新当前内容；手动保存、教师采用仍有效的 AI 预览、归档、
  恢复归档和历史恢复必须创建带原因的完整快照。AI 生成、重新生成、重试、拒绝和失败
  本身不得创建教案快照，只由任务、AI 结果和审计记录追踪。快照原因码固定为：
  `manual_save`、`ai_adopted`、`archive`、`unarchive`、`before_restore`、`restored`。
  恢复历史版本时，覆盖当前内容前必须先以 `before_restore` 保存当前内容；复制选定历史
  成为新的当前版本后，再以 `restored` 保存恢复完成后的内容。具体恢复行为同时遵循 FR-032。
```

验收标准：FR-031、FR-032、Spec Kit 数据模型和数据库 Schema 中的原因码完全一致。

### 1.3 补充系统架构 Q13，完成 M0-G1 内容同步

目标文件：`docs/design/system-architecture.md`

建议只增加摘要并引用契约，不在架构文档复制一套容易漂移的完整字段定义：

- 客户端幂等 scope 包含园所、操作者、HTTP 方法、规范化路由模板和 key。
- fingerprint 另含规范化实际 path、业务 query 和 canonical JSON body。
- 相同 scope、key 和 fingerprint 返回原结果。
- 相同 scope/key 但 fingerprint 不同返回冲突。
- 内部批量子任务不伪造客户端幂等键，使用自身业务唯一约束。
- 详细机器契约以 `specs/001-daily-activity-plan/contracts/openapi.yaml` 为准，跨端点语义和解释以 `specs/001-daily-activity-plan/contracts/README.md` 为准。

验收标准：系统架构能够导航到契约定义，同时没有形成第二套不一致规则。

### 1.4 修复 README 链接，解除当前 M0-G5 失败

目标文件：`README.md`

将错误的根目录链接改为：

```text
specs/001-daily-activity-plan/contracts/openapi.yaml
specs/001-daily-activity-plan/contracts/README.md
```

验收标准：Markdown 本地链接扫描结果为 0 个缺失链接。

## 2. P0：收敛状态和任务证据

内容修复完成后，再统一以下文件：

- `docs/faq/combined-audit.md`
- `docs/ROADMAP.md`
- `CONTEXT.md`
- `specs/001-daily-activity-plan/spec.md`
- `specs/001-daily-activity-plan/plan.md`
- `specs/001-daily-activity-plan/tasks.md`
- `docs/审查报告/codex-trae联合审查报告20260713.md`

建议处理方式：

1. Spec 不再声称已经 `Ready for Implementation`，明确 M0 仍为 `in_progress`。
2. Plan 删除“只剩分支授权”和旧单分支 cherry-pick 表述。
3. 将 T002 重新定义为“对最终候选文档基线执行 Pre-M1 复核”。旧提交 `56cc5e2` 只能作为历史记录，不能作为当前通过证据。
4. T002 在新验证完成前不应保持误导性的完成状态；建议重新打开为 `[ ]`，验证通过后再勾选。
5. 重写 T003：
   - 只能在 M0 完成后执行；
   - 经授权从同一最终 `main` 提交创建 `codex` 和 `trae`；
   - 两个分支记录相同基线提交 ID；
   - 不使用旧基线提交作为初始 cherry-pick 来源。
6. CONTEXT 和 Roadmap 只记录实际状态，不提前写 M0 完成或 M1 `ready`。
7. 联合审查报告保留原发现，并增加“修复验证记录”；不要直接删除历史问题描述。

建议将 T002/T003 重写为面向未来的任务，避免未完成任务继续使用“已完成”时态或预设未经授权的提交、推送结果：

```markdown
- [ ] T002 对最终候选文档基线执行 Pre-M1 一致性审查；验证：Spec Kit 分析无未解释的
  CRITICAL/HIGH 问题，72 个 FR 与 17 个 SC 均有任务映射，文档链接、模板结构/样式/哈希
  和 graphify 专项检查通过。完成前不得写入最终提交 ID，也不得声称已同步 `origin/main`。
- [ ] T003 在 M0-G1～M0-G8 全部关闭并形成最终 docs-only `main` 基线后，经明确授权从
  同一提交创建 `codex` 和 `trae` 两个实现分支，记录相同基线提交 ID；初始建分支不得
  cherry-pick 旧基线，后续新的共享文档提交才分别在授权后 cherry-pick。
```

## 3. P1：一次性执行专项验证

普通文档全部修改后统一验证，避免每修改一个文件都重建图谱。

基础检查：

```bash
git diff --check
sha256sum templates/teacherplan/teacherplan.docx
```

专项检查：

- Markdown 本地链接扫描，期望 0 个失败。
- 模板表格结构、字体、字号、段落和换行检查。
- 固定哈希引用全库检索。
- Spec Kit 一致性分析，要求不存在未解释的 CRITICAL/HIGH 冲突。
- FR、SC、Task 编号连续性和映射覆盖检查。
- 执行 `graphify update .`。
- 针对 M0、Q13、模板规则、T002/T003 执行 graphify 查询。
- graphify 完整性诊断应无缺失端点、悬空边、自环和重复边。

当前没有 Python 工程骨架，因此不为本轮纯文档修复伪运行 Ruff、Pyright 或 Pytest。工程骨架建立后再执行 `AGENTS.md` 规定的标准命令。

## 4. P2：独立处理 M0-G7 历史隐私清理

这一步不得与普通 Markdown 修改混合，也不得通过删除当前文件声称历史已经清理。

执行前必须单独确认：

- 需要清理的历史对象范围。
- 所有本地和远程分支、标签。
- 协作者停止推送的协调窗口。
- 清理后的强制更新方式。
- 旧克隆、Fork、缓存和 CI 产物的处置。
- 清理后的重新扫描和重新克隆验证。
- 如何在干净历史上保留本轮普通文档修改。

历史改写必须获得单独明确授权。建议使用专用镜像克隆执行，不在当前脏工作区直接试验。

## 5. P2：形成 M0-G8 共享基线

M0-G1～M0-G7 均具备可复现的通过证据后：

1. 经授权形成单一目的 docs-only `main` 基线。
2. 记录最终提交 ID。
3. 将 M0 标记为 `complete`、M1 标记为 `ready`。

历史清理和共享基线都属于 P2，但授权相互独立：历史清理授权不自动授权提交或推送最终基线。

## 6. P3：启动 M1 独立实现

M0 已标记为 `complete`、M1 已标记为 `ready` 后：

1. 经再次授权从同一最终提交创建 `codex` 和 `trae` 两个分支。
2. 两个分支分别记录相同的共享基线提交 ID。
3. 经授权创建一个 M1 共享父 Issue、一个 Codex 实现子 Issue和一个 Trae 实现子 Issue。
4. 两个实现分支独立开发，不互相合并或 cherry-pick 实现代码。

## 7. 建议任务拆分

建议将执行工作拆成四个相互有依赖但验收独立的任务：

| 任务 | 范围                                          | 完成证据                            |
| ---- | --------------------------------------------- | ----------------------------------- |
| A    | 修复模板说明、FR-031、Q13 和 README           | 目标规则检索通过，链接错误归零      |
| B    | 同步状态、Plan 和 T001～T003                  | 状态语义一致，双 Agent 流程无旧证据 |
| C    | 运行 Spec Kit、模板、链接和 graphify 专项验证 | 所有专项检查通过且结果可复现        |
| D    | 经单独授权处理历史清理与最终共享基线          | 历史复核通过，最终基线 ID 明确      |

任务 A～C 属于普通文档收敛；任务 D 包含高风险和 Git 操作，必须分开授权。

## 8. 授权边界

以下操作必须分别获得用户明确授权：

1. Git 历史隐私清理及远程历史更新。
2. 最终 docs-only 基线提交和推送。
3. 创建、切换或同步 `codex`、`trae` 实现分支。
4. 创建或更新 GitHub Issue。

完成 M0 内容门禁不自动获得上述操作权限。
